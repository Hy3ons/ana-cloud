# Nginx Configuration

이 디렉토리는 VM 환경에서 Nginx를 설정하기 위한 파일들을 관리합니다.

## 주요 특징
- **설치**: `apt install nginx` 명령어를 통해 설치된 Nginx 환경을 기반으로 합니다.
- **라우팅**: `/api` 경로로 들어오는 요청을 식별하여 적절한 서비스로 전달하는 리버스 프록시 설정을 포함합니다.
- **구성**: VM내에서 동작하는 Nginx의 라우팅 및 서버 설정 파일들이 저장되어 있습니다.
